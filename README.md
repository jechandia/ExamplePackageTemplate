# ExamplePackageTemplate

> **GitHub Template** — When you create a new repository from this template, a GitHub Actions workflow will automatically rename all occurrences of `Example` and `ExamplePackageTemplate` to your new repository name (files, directories, and code).
